// Give you a function animal, accept 1 parameter:obj like this:

function animal(obj){
    let name = obj.name 
    let leg = obj.legs
    let color = obj.color
    
 return  `This ${color} ${name} has ${leg} legs.`
 }
 
 